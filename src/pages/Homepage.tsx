import { Typography } from "@mui/material"
import { Box } from "@mui/system"

export const Homepage = () => {
    return <>
    <Box display={'flex'} flexDirection={'column'}>
        <Typography variant="h3">This is Paseen Homepage, Nothing to see here.</Typography>
    </Box>
    </>
}